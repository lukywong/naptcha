{
  "name": "naptcha",
  "version": "1.0.2",
  "description": "A simple captcha for nodejs",
  "main": "index.js",
  "scripts": {
    "config": "node-gyp configure",
    "build": "node-gyp configure && node-gyp build && rm -rf ./dist && mkdir ./dist && babel -d ./dist ./lib",
    "demo": "node-gyp configure && node-gyp build && babel -d ./dist ./lib && node ./demo/test.js"
  },
  "author": "lukywong@gmail.com",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.1.0",
    "curry": "^1.2.0",
    "object-assign": "^4.0.1"
  },
  "devDependencies": {
    "babel": "^5.8.23"
  }
}
